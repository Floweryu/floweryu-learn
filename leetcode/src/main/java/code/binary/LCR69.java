package code.binary;

/**
 * @author: zhangjunfeng
 * @date: 2023/12/15
 */
public class LCR69 {
    public int peakIndexInMountainArray(int[] arr) {
        int i = 0, j = arr.length;
        while (i < j) {
            int mid = (i + j) / 2;
            if (arr[mid] > arr[mid + 1]) {

            }
        }
        return 0;
    }
}
